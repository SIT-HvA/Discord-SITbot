{
  "name": "discord-sitbot",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "deploy": "node deploy-commands.js",
    "check": "node check.js",
    "prepare": "git config core.hooksPath .githooks || exit 0",
    "test": "node --test test/"
  },
  "dependencies": {
    "discord.js": "^14.16.3",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
